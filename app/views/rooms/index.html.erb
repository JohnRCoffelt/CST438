<div class="alert alert-primary">
      <h4 class="alert-heading">
        Welcome to Peril!
      </h4>

      <p>
        To start a game, create a game room. 
        You can create or join a room from the selection below.
      </p>
    </div>
  </div>
</div>

<h3 class="text-muted mb-0">
  Game Rooms
</h3>

<hr class="mt-1">

<% if @rooms.present? %>
  <nav class="nav flex-column">
    <% @rooms.each do |room| %>
      <%= link_to room.name, room_path(room), class: 'nav-link room-nav-link' %>
    <% end %>
  </nav>
<% else %>
  <div class="text-muted">
    The are no rooms yet
  </div>
<% end %>

<div class="mt-3">
  <%= link_to new_room_path, class: 'btn btn-primary' do %>
    New Game Room
  <% end %>
</div>
